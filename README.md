# 基于SpringBoot的Websocket服务端

### 使用指南

- 引入本项目

```xml

<dependency>
    <groupId>cn.shuniverse</groupId>
    <artifactId>shuniverse-websocket</artifactId>
    <version>1.0-SNAPSHOT</version>
</dependency>
```
- 在项目启动类添加 `cn.shuniverse.websocket`包的扫描
- 请求路径 `ws://ip:port/ws/{uid}`
